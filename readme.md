# Getting Started

To install `serve` package run:

### `npm i --global serve`

## Available Scripts

In the project directory, you can run:

### `npm run buld`

Builds the app to the `dist` folder.\


### `npm run serve` 

Runs the server 